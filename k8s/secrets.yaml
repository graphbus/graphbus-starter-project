# IMPORTANT: Replace ALL base64 values below before deploying.
# Generate a base64 value: echo -n 'your-value' | base64
#
# Get your GRAPHBUS_API_KEY at https://graphbus.com/onboarding
# Then: echo -n 'gb_your_key_here' | base64
apiVersion: v1
kind: Secret
metadata:
  name: graphbus-secrets
  namespace: graphbus-starter
type: Opaque
data:
  # Required — get your key at https://graphbus.com/onboarding
  # echo -n 'gb_your_key_here' | base64
  GRAPHBUS_API_KEY: REPLACE_WITH_BASE64_ENCODED_KEY
  # Generate: python -c "import secrets; print(secrets.token_urlsafe(32))" | base64
  SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
