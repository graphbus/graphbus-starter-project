# IMPORTANT: Replace the base64 values below before deploying.
# Generate a real secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Then base64-encode it: echo -n 'your-secret' | base64
apiVersion: v1
kind: Secret
metadata:
  name: graphbus-secrets
  namespace: graphbus-starter
type: Opaque
data:
  # Replace before deploying — this is a placeholder
  SECRET_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
