# ── GraphBus API Key (REQUIRED) ─────────────────────────────────────────────
# Get yours free at https://graphbus.com/onboarding
# The app will refuse to start without this.
GRAPHBUS_API_KEY=gb_...

# ── App ──────────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-in-production
DATABASE_URL=sqlite:///./data/graphbus.db
CORS_ORIGINS=http://localhost:3000

# ── LLM Provider (for build mode — pick one) ─────────────────────────────────
DEEPSEEK_API_KEY=          # recommended — deepseek-reasoner
ANTHROPIC_API_KEY=         # claude-sonnet
OPENROUTER_API_KEY=        # any model via OpenRouter
